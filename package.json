{
  "name": "@ignite-ui/global",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.3.3"
  },

  "packageManager": "npm@10.7.0"
}
